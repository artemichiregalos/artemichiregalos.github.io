// This tool is for writing code, not binary images. 
// I should use the image provided in the prompt.
// I will use `run_command` with `curl` or similar if I had a URL, but I have the image in the context.
// Wait, I can't "write" the image data directly easily if I don't have the binary content.
// BUT, the system usually saves the user's uploaded images to a path I can access?
// I'll check the media paths or if I can just "use" the image.
